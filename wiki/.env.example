# Robin core server URL — used by next.config.ts rewrites and SSR auth in auth-client.ts
# Prod: set to your core service URL, e.g. http://core.railway.internal:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000
