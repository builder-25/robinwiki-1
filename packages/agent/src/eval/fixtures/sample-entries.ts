import type { RawEntry } from '@robin/shared'

export const sampleEntries: RawEntry[] = [
  {
    content:
      'Atomic habits are small changes that compound over time. A 1% improvement each day results in 37x improvement over a year. The key insight is that systems beat goals — focus on the process, not the outcome.',
    source: 'api',
  },
]
